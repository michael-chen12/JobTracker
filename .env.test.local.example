# Playwright E2E test credentials
# Copy this file to .env.test.local and fill in real values.
# NEVER commit .env.test.local to version control!
#
# The test user MUST already exist in your Supabase project with
# email/password authentication enabled. Create it via:
#   Supabase Dashboard → Authentication → Users → Add user

TEST_USER_EMAIL=test@yourapp.com
TEST_USER_PASSWORD=YourTestPassword123!
